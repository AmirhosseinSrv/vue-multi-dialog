const DIALOG_WRAPPER =
  'fixed inset-0 flex justify-center items-start bg-gray-300 bg-opacity-70 rounded-md px-4 py-6 overflow-y-auto';

const MULTI_DIALOG_WRAPPER =
  'fixed top-1/2 flex w-fit h-fit justify-center items-start shadow-2xl rounded-md overflow-y-auto';

const DIALOG_EXPANDABLE =
  'mx-4 text-gray-600 cursor-pointer flex items-center';

const DIALOG_HEADER =
  'bg-gray-200 h-12 flex items-center justify-between px-4 border-gray-100';

const MULTI_DIALOG_HEADER =
  'bg-gray-200 h-12 flex items-center justify-between px-4 cursor-move border-gray-100';

const DIALOG_BASE =
  'm-auto w-fit min-w-[190px] bg-gray-100 overflow-hidden lg:w-fit';

export {
  DIALOG_BASE,
  DIALOG_WRAPPER,
  MULTI_DIALOG_WRAPPER,
  DIALOG_HEADER,
  MULTI_DIALOG_HEADER,
  DIALOG_EXPANDABLE,
};
